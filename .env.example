# Futures Movement Predictor - Environment Variables
# Copy this file to .env and update the values

# ==============================================================================
# Application Settings
# ==============================================================================

# Node environment (development|production)
NODE_ENV=production

# Server port (default: 5000)
PORT=5000

# Timezone for market hours and scheduler (default: America/New_York)
TZ=America/New_York

# ==============================================================================
# Database Configuration
# ==============================================================================

# PostgreSQL connection string
# Format: postgres://username:password@host:port/database
# Example (development): postgres://futures:futures_password@localhost:5432/futures_db
# Example (docker): postgres://futures:futures_password@postgres:5432/futures_db
DATABASE_URL=postgres://futures:futures_password@postgres:5432/futures_db

# ==============================================================================
# Security
# ==============================================================================

# Session secret for Express session (CHANGE THIS IN PRODUCTION!)
# Generate a random string: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-random-secret-in-production

# ==============================================================================
# Scheduler Configuration
# ==============================================================================

# Enable/disable nightly scheduler (true|false)
# Set to "false" for web service, "true" for dedicated scheduler service
SCHEDULER_ENABLED=true

# ==============================================================================
# Docker Compose Configuration (docker-compose.yml)
# ==============================================================================

# These are referenced in docker-compose.yml and can be overridden here:

# Database credentials (used by postgres service)
POSTGRES_USER=futures
POSTGRES_PASSWORD=futures_password
POSTGRES_DB=futures_db

# ==============================================================================
# Yahoo Finance Configuration
# ==============================================================================

# No API key required! Yahoo Finance is free
# The application automatically fetches daily closing prices for:
# - /NQ (E-mini Nasdaq-100)
# - /ES (E-mini S&P 500)
# - /YM (E-mini Dow Jones)
# - /RTY (E-mini Russell 2000)
# - /GC (Gold Futures)
# - /CL (Crude Oil Futures)

# ==============================================================================
# Notes
# ==============================================================================

# 1. For production deployment, ALWAYS change SESSION_SECRET to a strong random value
# 2. Use strong passwords for POSTGRES_PASSWORD in production
# 3. The scheduler runs automatically at 5:30 PM ET (after market close)
# 4. Database migrations run automatically when containers start
# 5. Web service and scheduler use the same codebase but different environment flags
